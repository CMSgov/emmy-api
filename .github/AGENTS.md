# .github AGENTS

## Scope

These instructions apply to work inside `.github/`, especially
`.github/workflows/`.

## Workflow Runner Policy

For workflow runner requirements and related safety constraints, follow the
repository root `AGENTS.md` as the single source of truth.

When working in `.github/workflows/`, do not duplicate or restate that policy
here; apply the root guidance directly.
