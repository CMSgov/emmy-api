# Documentation Accuracy Review (as of April 08, 2026)

## Section A: Executive Summary

Score: `92/100`

Risk statement: The main drift found in this run was concentrated in runtime
and security docs that still described retired `/api/edu`, `/status`, and
Cognito/JWKS auth behavior. Those mismatches were repaired in this run, but the
branch still has an intentional contract-vs-runtime gap around bearer-token
enforcement that must remain clearly caveated in docs.

Gate: `PASS`

The current branch's design-time OpenAPI contract is internally consistent. The
highest-risk documentation drift found during the audit was operational:
readers would have been told to expect Cognito token validation and legacy
routes that are not present in the current branch. The affected runtime,
security, setup, guide, and agent docs were updated during this run.

## Section B: Severity-Ranked Findings

| Severity | Doc location | Observed mismatch | Source-of-truth evidence on current branch | Recommended fix |
|---|---|---|---|---|
| `P1` | `docs/api.md:11`; `docs/api.md:28`; `docs/api.md:36`; `docs/api.md:43`; `docs/api.md:95`; `docs/api.md:123` | The runtime API page still documents Cognito header validation, `GET /api/edu`, and `GET /status`, even though the current branch serves `GET /health`, `POST /api/v0/education-enrollments`, and `POST /api/v0/veteran-disability-ratings`. | `api/app.go:124`; `api/app.go:126`; `api/routes/status_router.go:24`; `api/routes/router.go:24`; `api/routes/router.go:45`; `api/routes/router.go:46`; `api/handlers/education_handler.go:43`; `api/handlers/veteran_handler.go:42` | Rewrite the runtime endpoint, auth, and example sections to match the current Fiber wiring and distinguish runtime behavior from the contract-first OpenAPI docs. |
| `P1` | `docs/overview.md:5`; `docs/overview.md:24`; `docs/overview.md:31`; `docs/overview.md:33`; `docs/overview.md:53`; `docs/overview.md:64`; `docs/architecture.md:56`; `docs/architecture.md:83`; `docs/architecture.md:89`; `docs/architecture.md:105` | Overview and architecture still describe Cognito-based auth and a scaffolded `/api/edu` route instead of the current POST education route and local skip-auth middleware. | `api/app.go:99`; `api/app.go:101`; `api/app.go:110`; `api/app.go:115`; `api/app.go:121`; `api/app.go:126`; `api/routes/router.go:45`; `api/routes/router.go:46`; `api/routes/status_router.go:24` | Update route inventories, middleware descriptions, diagrams, and caveats to reflect the current branch's middleware stack and runtime paths. |
| `P1` | `docs/features/security/cognito-auth.md:1`; `docs/features/README.md:27`; `docs/research/auth-token-validation-strategy.md:5`; `docs/research/auth-token-validation-strategy.md:9` | Security feature and research docs present Cognito/JWKS auth as current implementation, but the repository no longer contains Cognito verifier code or Cognito config loading. The only auth-related middleware on this branch injects local identity when `SKIP_AUTH=true`. | `api/middleware/middleware.go:16`; `api/middleware/middleware.go:24`; `api/middleware/middleware.go:50`; `api/app.go:126`; `pkg/core/config.go:101`; `pkg/core/config.go:116` | Replace the stale Cognito docs with branch-accurate skip-auth middleware documentation and recast the auth research page as historical/deferred research instead of current runtime behavior. |
| `P2` | `docs/setup.md:6`; `docs/setup.md:18`; `docs/setup.md:91` | Setup docs say the compose file provides only API and observability services and list Cognito env vars that `core.NewConfigFromEnv` does not read. | `docker-compose.yml:1`; `docker-compose.yml:11`; `pkg/core/config.go:101`; `pkg/core/config.go:116` | Remove the obsolete Cognito env-var table row and describe the actual compose stack, including the checked-in Redis service. |
| `P2` | `AGENTS.md:34` | `AGENTS.md` points agents at `api-spec/openapi.yaml`, but the current versioned contract entrypoint is `api-spec/v0/openapi.yaml`. | `api-spec/v0/openapi.yaml:1`; `api-spec/README.md:11` | Update the repository overview to reference the versioned OpenAPI source path used on this branch. |

## Section C: Update Backlog Checklist by Doc File

- `docs/api.md`
  - [x] Replace legacy route/auth descriptions with current runtime behavior.
  - [x] Remove the duplicate stale endpoint table.
  - [x] Update examples for `POST /api/v0/education-enrollments` and `POST /api/v0/veteran-disability-ratings`.
- `docs/overview.md`
  - [x] Replace Cognito references with branch-accurate skip-auth/runtime wording.
  - [x] Update the route summary and Mermaid flow to the current POST education route.
- `docs/architecture.md`
  - [x] Fix middleware order, route inventory, and handler timeout notes.
- `docs/features/README.md`
  - [x] Remove or rename the stale Cognito feature entry.
- `docs/features/security/cognito-auth.md`
  - [x] Replace with a branch-accurate security feature page or remove from the feature index.
- `docs/research/auth-token-validation-strategy.md`
  - [x] Reframe as historical/deferred auth research instead of current implementation.
- `docs/setup.md`
  - [x] Remove the nonexistent Cognito env vars from the setup table.
  - [x] Describe the committed Redis service in `docker-compose.yml`.
- `docs/guides/01-getting-started.md`
  - [x] Add a contract-vs-runtime caveat so bearer-token examples are not misread as current middleware behavior.
- `docs/guides/02-authentication.md`
  - [x] Clarify that OAuth2 client credentials remain contract-first documentation on this branch.
- `docs/guides/03-usage-examples.md`
  - [x] Clarify that examples mirror the checked-in contract while runtime auth remains unfinished.
- `AGENTS.md`
  - [x] Update the observed OpenAPI entrypoint to `api-spec/v0/openapi.yaml`.

## Section D: Hygiene Appendix

Commands run:

- `git status --short --branch`
- `git log --oneline -n 10`
- `date +%F_%T`
- `rg --files docs api pkg api-spec .github README.md AGENTS.md main.go`
- `sed -n '1,260p' README.md`
- `sed -n '1,260p' docs/setup.md`
- `sed -n '1,260p' docs/overview.md`
- `sed -n '1,260p' docs/api.md`
- `sed -n '1,260p' docs/architecture.md`
- `sed -n '1,260p' docs/guides/01-getting-started.md`
- `sed -n '1,260p' docs/guides/02-authentication.md`
- `sed -n '1,260p' docs/guides/03-usage-examples.md`
- `sed -n '1,320p' api/middleware/middleware.go`
- `sed -n '1,260p' api/routes/router.go`
- `sed -n '1,260p' api/routes/status_router.go`
- `sed -n '1,260p' api/handlers/education_handler.go`
- `sed -n '1,260p' api/handlers/veteran_handler.go`
- `sed -n '1,220p' api/handlers/openapi_spec_handler.go`
- `sed -n '1,260p' api-spec/README.md`
- `sed -n '1,260p' api-spec/v0/openapi.yaml`
- `sed -n '1,240p' docs/features/security/cognito-auth.md`
- `sed -n '1,220p' docs/research/auth-token-validation-strategy.md`
- `sed -n '1,220p' docker-compose.yml`
- `git ls-files | rg -n '\\.(pem|key|p12|pfx)$'`
- `git ls-files -z | xargs -0 rg -n 'BEGIN (RSA |EC |OPENSSH |)?PRIVATE KEY'`
- `pnpm exec markdownlint-cli2 AGENTS.md README.md api-spec/README.md schema/README.md 'docs/**/*.md'`

Sensitive-file scan:

- No tracked PEM/key/private-key artifacts were detected by filename pattern.
- No tracked file contained a PEM private-key block header.
- Repository root still contains `public.jwk`, which is expected and already
  called out as a sensitive root file that should not be modified without
  approval.

Markdown validation:

- `pnpm exec markdownlint-cli2 AGENTS.md README.md api-spec/README.md schema/README.md 'docs/**/*.md'`
  passed with `0 error(s)` across `33 file(s)`.

Hygiene verdict: `PASS` after remediation in this run.

## Section E: Deferred Watchlist (Non-blocking)

- If branch work restores real bearer-token validation, update:
  `docs/api.md`, `docs/overview.md`, `docs/architecture.md`,
  `docs/features/security/*`, and `docs/research/auth-token-validation-strategy.md`.
- If the OpenAPI contract and runtime are intentionally allowed to diverge for a
  period, maintain explicit "contract vs current runtime" caveats in
  `docs/guides/*.md` and `docs/api.md`.
- If `api-spec/v0/dist/` or `api-spec/dist/v0/` bundle locations change, update
  `api-spec/README.md`, `docs/api.md`, and any spec-serving notes that mention
  the runtime artifact path.
