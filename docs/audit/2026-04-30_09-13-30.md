# Documentation Accuracy Review (as of April 30, 2026)

## Section A: Executive Summary

**Report date:** `2026-04-30`
**Scope:** `root docs + docs/**/*.md`
**Overall accuracy score:** **98/100**
**Completion gate status:** **PASS**

**Risk statement:**

- Primary contributor-facing and runtime-facing documentation drift identified in earlier audits has been corrected on the current branch.
- The remaining risk is operational, not documentary: `mise.toml` still advertises aggregate contract tasks that depend on shell wrappers not checked into `scripts/`, so future doc updates should continue to describe only the commands that are actually runnable.
- Markdown hygiene and local relative-link validation are clean for the audited scope.

## Section B: Severity-Ranked Findings

No `P1` findings were identified in this audit rerun.

No `P2` findings were identified in this audit rerun.

| Severity | Doc location | Observed mismatch | Source-of-truth evidence on current branch | Recommended fix |
|---|---|---|---|---|
| `P3` | `README.md:36`; `README.md:64`; `api-spec/README.md:45`; `docs/planning/public-api-data-model-design-first.md:160` | Contract-tooling docs now describe only the checked-in bundle/validate/lint/format/schema commands, but `mise.toml` still contains aggregate tasks that reference missing shell wrappers for YAML linting and OpenAPI breaking checks. | `mise.toml:17`; `mise.toml:45`; `package.json:5`; `package.json:6`; `package.json:7` | Keep contributor docs limited to runnable commands until the missing wrappers are restored, or add the absent wrapper scripts before expanding the docs again. |

## Section C: Update Backlog Checklist by Doc File

### `README.md`

- [x] Limit local contract-tooling instructions to commands that exist on this branch.

### `api-spec/README.md`

- [x] Replace missing shell-wrapper examples with current `pnpm` and `mise` entry points.

### `docs/planning/public-api-data-model-design-first.md`

- [x] Update the local bundle/validate/lint examples to match the checked-in tooling.

### `docs/setup.md`

- [x] Align local dependency and environment-variable guidance with `docker-compose.yml` and `pkg/core/config.go`.

### `docs/api.md`

- [x] Rewrite the runtime route, auth, and circuit-breaker notes to match `api/app.go`, `api/routes/router.go`, and `api/middleware/middleware.go`.

### `docs/overview.md`

- [x] Refresh route inventory and request-flow diagram for the current runtime.

### `docs/architecture.md`

- [x] Correct middleware order, interface names, timeout notes, and current route caveats.

### `docs/features/core/nsc-education.md`

- [x] Remove the stale claim that the handler still ignores caller-provided payloads.

### `docs/features/infrastructure/redis.md`

- [x] Remove the stale Redis-injection caveat now that `main.run` passes Redis into `api.New`.

### `docs/features/security/cognito-auth.md`

- [x] Reframe the page around current request identity handling rather than nonexistent Cognito verification middleware.

### `docs/research/auth-token-validation-strategy.md`

- [x] Mark Cognito/JWKS verification as a future strategy instead of current behavior.

### `docs/guides/01-getting-started.md`

- [x] Fix public contract route examples to use `/api/v0/...`.

### `AGENTS.md`

- [x] Correct the observed OpenAPI entry-point path to `api-spec/v0/openapi.yaml`.

## Section D: Hygiene Appendix

### Commands run

- `date +%F_%H-%M-%S`
- `git status --short --branch`
- `rg --files -g 'README.md' -g 'docs/**/*.md' -g 'AGENTS.md'`
- `sed -n '1,260p' main.go`
- `sed -n '1,260p' api/app.go`
- `sed -n '1,260p' api/routes/router.go`
- `sed -n '1,220p' api/routes/status_router.go`
- `sed -n '1,320p' api/middleware/middleware.go`
- `sed -n '1,260p' api/handlers/education_handler.go`
- `sed -n '1,260p' api/handlers/veteran_handler.go`
- `sed -n '1,260p' pkg/core/config.go`
- `sed -n '1,260p' pkg/education/service.go`
- `sed -n '1,260p' pkg/veteran/service.go`
- `sed -n '1,220p' mise.toml`
- `sed -n '1,260p' package.json`
- `ls -1 scripts | sort`
- `for p in scripts/bundle-api-spec scripts/validate-api-spec scripts/lint-api-spec scripts/lint-yaml-files scripts/check-openapi-breaking scripts/check-format-contract-files scripts/check-json-schemas; do if [ -e "$p" ]; then echo "OK $p"; else echo "MISSING $p"; fi; done`
- `rg -n '/api/edu|/status|bundle-api-spec|validate-api-spec|lint-api-spec|lint-yaml-files|check-openapi-breaking' README.md api-spec/README.md AGENTS.md docs/**/*.md`
- `pnpm exec markdownlint-cli2 README.md AGENTS.md api-spec/README.md "docs/**/*.md"`
- `zsh -lc 'setopt null_glob; for f in README.md AGENTS.md api-spec/README.md docs/**/*.md; do perl -ne '\''while(/\[[^\]]+\]\(([^)#]+)(?:#[^)]+)?\)/g){print qq{$ARGV\t$1\n} unless $1 =~ m{^(https?:|mailto:)} }'\'' "$f"; done | while IFS=$'\''\t'\'' read -r src link; do base=$(dirname "$src"); target="$base/$link"; if [ ! -e "$target" ]; then printf "%s -> %s\n" "$src" "$link"; fi; done'`
- `rg -n --hidden --no-ignore '(BEGIN (RSA|EC|OPENSSH|DSA) PRIVATE KEY|AKIA[0-9A-Z]{16}|ASIA[0-9A-Z]{16}|ghp_[A-Za-z0-9]{36}|xox[baprs]-[A-Za-z0-9-]+|aws_secret_access_key|AIza[0-9A-Za-z_-]{35})' $(git ls-files '*.md')`

### Sensitive-file scan findings

- Scoped scan of tracked Markdown files found no live private-key or credential material in the audited docs set.
- Historical audit reports may still contain secret-scan regex literals as documentation examples; no live secrets were identified in those files during this run.

### Markdown link/reference/path validation findings

- `markdownlint-cli2` passed for the audited Markdown scope.
- Local relative-link validation found no missing file targets in the audited docs set.

### Hygiene verdict

- **Pass:** The audited docs are aligned with the current branch well enough to pass, with only a low-risk watch item around unfinished aggregate contract tasks in `mise.toml`.

## Section E: Deferred Watchlist (Non-blocking)

- If verified bearer-token middleware is reintroduced, update:
  - `docs/api.md`
  - `docs/overview.md`
  - `docs/architecture.md`
  - `docs/features/security/cognito-auth.md`
  - `docs/research/auth-token-validation-strategy.md`
- If local wrapper scripts return for YAML linting or OpenAPI breaking checks, update:
  - `README.md`
  - `api-spec/README.md`
  - `docs/planning/public-api-data-model-design-first.md`
