# Documentation Accuracy Review (as of March 31, 2026)

## Section A: Executive Summary

**Report date:** `2026-03-31`
**Scope:** `root docs + docs/**/*.md`
**Overall accuracy score:** **71/100**
**Completion gate status:** **FAIL**

**Risk statement:**

- Primary runtime docs still describe `/status` and omit the live `POST /api/v0/veteran-disability-ratings` endpoint, which can mislead operators and integrators about what is actually deployed.
- Multiple docs still carry stale caveats saying `main` does not wire Redis into `api.New`, even though the current branch does inject Redis and serves `GET /health`.
- Root contributor/ownership docs and the README SBOM section still contain placeholders, so key project metadata remains non-actionable.

## Section B: Severity-Ranked Findings

| Severity | Doc location | Observed mismatch | Source-of-truth evidence on current branch | Recommended fix |
|---|---|---|---|---|
| `P1` | `docs/api.md:5`; `docs/api.md:20`; `docs/api.md:24`; `docs/api.md:34`; `docs/api.md:73`; `docs/api.md:87` | The runtime API page still says the server runs on port `8000` by default, that auth/circuit-breaker behavior applies to `/status`, and that `/status` is the live Redis health route. The current branch serves `GET /health` and `POST /api/v0/veteran-disability-ratings`, and `main` now injects Redis into `api.New`. | `pkg/core/config.go:10`; `api/routes/status_router.go:24`; `api/routes/router.go:45`; `api/routes/router.go:46`; `main.go:98`; `main.go:102` | Rewrite `docs/api.md` to match the current runtime surface: `GET /`, `GET /health`, `GET /api/edu`, and `POST /api/v0/veteran-disability-ratings`, and remove the stale nil-Redis caveat. |
| `P1` | `docs/overview.md:5`; `docs/overview.md:32`; `docs/overview.md:61`; `docs/overview.md:74`; `docs/architecture.md:67`; `docs/architecture.md:96`; `docs/architecture.md:100`; `docs/architecture.md:106` | The overview and architecture pages still describe the old `/status` route, omit the veteran endpoint from route/package summaries, and repeat the outdated claim that `main` does not inject Redis into `api.New`. | `api/routes/status_router.go:24`; `api/routes/router.go:45`; `api/routes/router.go:46`; `main.go:98`; `main.go:102`; `main.go:113`; `api/app.go:106` | Update the route inventory, request-flow diagrams, and DI notes so they reflect `GET /health`, the veteran disability POST endpoint, and the current Redis wiring. |
| `P2` | `docs/setup.md:16`; `docs/setup.md:17`; `docs/setup.md:79` | Setup docs present inaccurate defaults for `PORT` and `OTEL_DISABLE`, and still advertise the API on `:8000` as if that were the runtime default. The code defaults are `PORT=3000` and `OTEL_DISABLE=true`; `.env.example` overrides those for local example use. | `pkg/core/config.go:10`; `pkg/core/config.go:12`; `.env.example:1`; `.env.example:5` | Split “code defaults” from “example `.env` values” in `docs/setup.md`, and stop describing `:8000` as the default listener unless the doc explicitly says it comes from `.env.example`. |
| `P2` | `README.md:83` | The README SBOM section still contains a cookiecutter placeholder URL, so the advertised SBOM/dependency destination is broken. | `README.md:83` | Replace the placeholder with the real dependency-graph/SBOM URL or remove the sentence until one exists. |
| `P2` | `CONTRIBUTING.md:1`; `CONTRIBUTING.md:5`; `COMMUNITY.md:3`; `COMMUNITY.md:7`; `COMMUNITY.md:13` | Contributor and ownership docs are still template shells with TODO comments and placeholder table values, despite the repo having real setup, lint, and test workflows elsewhere. | `docs/setup.md:1`; `README.md:15`; `.pre-commit-config.yaml:1`; `mise.toml:1` | Replace template content with actual contribution steps, issue/reporting guidance, and real project contacts/affiliations. |

## Section C: Update Backlog Checklist by Doc File

### `docs/api.md`

- [ ] Replace `/status` with `GET /health` and add `POST /api/v0/veteran-disability-ratings` to the runtime endpoint table.
- [ ] Correct the default-port language so it distinguishes code defaults from `.env.example`.
- [ ] Remove the stale caveat claiming `main` fails to inject Redis into `api.New`.

### `docs/overview.md`

- [ ] Update the package summary and request-flow diagram to use `GET /health` instead of `/status`.
- [ ] Add the veteran disability route to the current runtime summary.
- [ ] Remove the outdated Redis-wiring caveat.

### `docs/architecture.md`

- [ ] Update request lifecycle examples to reference `/health` and the veteran POST handler.
- [ ] Fix the dependency-injection section to show that `main` passes `Redis` into `api.New`.
- [ ] Remove the stale status-route caveat and replace it with current runtime caveats only.

### `docs/setup.md`

- [ ] Correct the documented config defaults for `PORT` and `OTEL_DISABLE`.
- [ ] Clarify that `.env.example` intentionally sets `PORT=8000` for local convenience while code defaults remain `3000`.
- [ ] Reword the Docker workflow section so `:8000` is described as the compose/example port, not the runtime default.

### `README.md`

- [ ] Replace or remove the SBOM placeholder URL in the SBOM section.

### `CONTRIBUTING.md`

- [ ] Replace the template headings with concrete contribution, setup, test, and issue-reporting instructions grounded in this repo.

### `COMMUNITY.md`

- [ ] Replace placeholder project/member text and TODO comments with actual project contacts and affiliations.

## Section D: Hygiene Appendix

### Commands run

- `date +%F_%H:%M:%S`
- `git status --short`
- `rg --files -g '*.md' . | sort`
- `sed -n '1,260p' main.go`
- `sed -n '1,240p' api/app.go`
- `sed -n '1,220p' api/routes/router.go`
- `sed -n '1,220p' api/routes/status_router.go`
- `sed -n '1,260p' pkg/core/config.go`
- `sed -n '1,220p' docs/setup.md`
- `sed -n '1,220p' docs/overview.md`
- `sed -n '1,260p' docs/architecture.md`
- `sed -n '1,260p' docs/api.md`
- `sed -n '1,240p' README.md`
- `sed -n '1,220p' CONTRIBUTING.md`
- `sed -n '1,220p' COMMUNITY.md`
- `markdownlint-cli2 README.md CONTRIBUTING.md COMMUNITY.md AGENTS.md SECURITY.md api-spec/README.md schema/README.md "docs/**/*.md"`
- `rg -n --hidden --no-ignore "(BEGIN (RSA|EC|OPENSSH|DSA) PRIVATE KEY|AKIA[0-9A-Z]{16}|ASIA[0-9A-Z]{16}|ghp_[A-Za-z0-9]{36}|xox[baprs]-[A-Za-z0-9-]+|aws_secret_access_key|AIza[0-9A-Za-z_-]{35})" $(git ls-files '*.md')`
- `zsh -lc 'setopt null_glob; for f in README.md CONTRIBUTING.md COMMUNITY.md AGENTS.md SECURITY.md api-spec/README.md schema/README.md docs/**/*.md; do perl -ne '\''while(/\[[^\]]+\]\(([^)#]+)(?:#[^)]+)?\)/g){print qq{$ARGV\t$1\n} unless $1 =~ m{^(https?:|mailto:)} }'\'' "$f"; done | while IFS=$'\''\t'\'' read -r src link; do base=$(dirname "$src"); target="$base/$link"; if [ ! -e "$target" ]; then printf "%s -> %s\n" "$src" "$link"; fi; done'`

### Sensitive-file scan findings

- Scoped scan of tracked Markdown files found no live credential or private-key material.
- The only matches were prior audit reports that contain the scan regex as literal text.

### Markdown link/reference/path validation findings

- `markdownlint-cli2` passed for the audited Markdown scope with `0 error(s)`.
- Local relative-link/path validation found no missing file targets in the audited docs.
- One content issue remains despite passing syntax/path validation: `README.md:83` contains a malformed placeholder SBOM URL.

### Hygiene verdict

- **Non-blocker:** Markdown hygiene is clean in the audited scope, but content accuracy still fails because unresolved `P1` and `P2` findings remain.

## Section E: Deferred Watchlist (Non-blocking)

- If the runtime is brought into full alignment with the v0 OpenAPI contract, update:
  - `docs/overview.md`
  - `docs/api.md`
  - `docs/architecture.md`
- If deployment/environment guidance is moved into tracked repository docs, update:
  - `AGENTS.md`
  - `README.md`
  - `docs/setup.md`
- If contributor ownership is finalized, update:
  - `COMMUNITY.md`
  - `README.md`
