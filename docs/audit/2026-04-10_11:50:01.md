# Documentation Accuracy Review (as of April 10, 2026)

## Section A: Executive Summary

Score: `74/100`

Risk statement: Primary runtime and setup docs had drifted far enough from the
current Go wiring to mislead contributors about live routes, auth behavior, and
supported contract-maintenance commands.

Gate: `FAIL`

This audit compared the root docs set and key repository markdown files against
the checked-out Go runtime, config, scripts, and contract artifacts. The
highest-impact mismatches were stale route/auth claims in runtime docs,
references to helper scripts that do not exist in `scripts/`, and a stale
versionless OpenAPI path in `AGENTS.md`.

## Section B: Severity-Ranked Findings

| Severity | Doc location | Observed mismatch | Source-of-truth evidence on current branch | Recommended fix |
|---|---|---|---|---|
| `P1` | `docs/api.md:13`; `docs/api.md:22`; `docs/api.md:28`; `docs/api.md:40`; `docs/api.md:46`; `docs/api.md:95`; `docs/api.md:123` | The runtime API page still described `GET /api/edu`, `GET /status`, and active Cognito header validation, but the current branch registers `GET /health`, `GET /api-spec/v1/verify`, `POST /api/v0/education-enrollments`, and `POST /api/v0/veteran-disability-ratings`, while `api.New` does not wire a Cognito verifier. | `api/routes/status_router.go:24`; `api/routes/router.go:24`; `api/routes/router.go:45`; `api/routes/router.go:46`; `api/app.go:124`; `api/app.go:126`; `api/handlers/education_handler.go:43` | Rewrite the runtime route/auth sections so they match the live endpoint table and clearly distinguish contract auth from current runtime wiring. |
| `P1` | `docs/overview.md:24`; `docs/overview.md:31`; `docs/overview.md:54`; `docs/architecture.md:54`; `docs/architecture.md:89`; `docs/architecture.md:96` | The overview and architecture pages still referred to `/api/edu` and Cognito runtime enforcement even though the current implementation exposes versioned POST routes and only conditionally injects local skip-auth identity. | `api/routes/router.go:24`; `api/routes/router.go:45`; `api/routes/router.go:46`; `api/app.go:99`; `api/app.go:101`; `api/app.go:124`; `api/app.go:126`; `api/middleware/middleware.go:29` | Update route inventories, flow diagrams, and middleware notes to reflect the actual runtime topology on this branch. |
| `P2` | `README.md:32`; `README.md:37`; `README.md:40`; `README.md:43`; `README.md:46`; `README.md:55`; `api-spec/README.md:45`; `api-spec/README.md:48`; `api-spec/README.md:49`; `api-spec/README.md:50`; `docs/planning/public-api-data-model-design-first.md:162`; `docs/planning/public-api-data-model-design-first.md:164`; `docs/planning/public-api-data-model-design-first.md:167`; `docs/planning/public-api-data-model-design-first.md:170` | Multiple docs instructed contributors to run shell helpers such as `./scripts/bundle-api-spec`, `./scripts/validate-api-spec`, `./scripts/lint-api-spec`, and `./scripts/check-openapi-breaking`, but those wrapper files are not present in `scripts/`. | `scripts/check-format-contract-files:1`; `scripts/check-json-schemas:1`; `mise.toml:4`; `mise.toml:8`; `mise.toml:12`; `mise.toml:36`; `package.json:4`; `package.json:5`; `package.json:6` | Replace missing shell-wrapper instructions with the supported `mise run ...` and `pnpm run ...` task names, and only mention checked-in shell scripts that actually exist. |
| `P2` | `docs/setup.md:6`; `docs/setup.md:18`; `docs/setup.md:131` | The setup page said the compose stack only provided API and observability services, listed Cognito env vars that `core.NewConfigFromEnv` does not read, and implied OTel is enabled by default even though `OTEL_DISABLE` defaults to `true`. | `docker-compose.yml:1`; `docker-compose.yml:14`; `pkg/core/config.go:11`; `pkg/core/config.go:100`; `pkg/core/config.go:106`; `pkg/core/config.go:110` | Update the compose description, environment-variable table, and telemetry note to match code/config defaults. |
| `P2` | `AGENTS.md:44` | `AGENTS.md` still pointed at `api-spec/openapi.yaml`, but the current checked-in contract source is versioned at `api-spec/v0/openapi.yaml`. | `api-spec/v0/openapi.yaml:1`; `api-spec/README.md:13` | Repair the entry-point reference so agent guidance uses the actual contract path on the branch. |
| `P3` | `docs/features/core/nsc-education.md:63`; `docs/features/core/nsc-education.md:70`; `docs/features/security/cognito-auth.md:5`; `docs/features/security/cognito-auth.md:9`; `docs/features/security/cognito-auth.md:44`; `docs/features/README.md:27` | Feature docs still said the education handler did not accept caller input and described a live Cognito verifier that is not wired in the current runtime. | `api/handlers/education_handler.go:43`; `api/handlers/education_handler.go:50`; `api/app.go:124`; `api/app.go:126`; `api/middleware/middleware.go:29` | Reframe these pages as runtime-accurate feature notes: the education route now parses request bodies, while Cognito enforcement is currently design intent rather than live branch behavior. |

## Section C: Update Backlog Checklist by Doc File

### `README.md`

- [ ] Replace missing shell-wrapper commands with supported `mise` and `pnpm`
  tasks.

### `api-spec/README.md`

- [ ] Point spec maintenance instructions at supported task runners instead of
  missing shell wrappers.

### `docs/planning/public-api-data-model-design-first.md`

- [ ] Align local contract-maintenance instructions with the commands actually
  available in `mise.toml` and `package.json`.

### `docs/setup.md`

- [ ] Fix the compose stack description.
- [ ] Remove env-var claims not backed by `pkg/core/config.go`.
- [ ] Correct the default OTel behavior note.

### `docs/api.md`

- [ ] Replace stale route table entries.
- [ ] Clarify current runtime auth wiring versus public-contract auth.

### `docs/overview.md`

- [ ] Update route summary and request-flow diagram to the versioned POST routes.
- [ ] Remove the claim that Cognito validation is active in the current branch.

### `docs/architecture.md`

- [ ] Correct timeout, middleware, and runtime-route notes.

### `docs/features/core/nsc-education.md`

- [ ] Remove stale references to a hardcoded HTTP-handler payload.

### `docs/features/security/cognito-auth.md`

- [ ] Mark Cognito enforcement as deferred runtime work until a verifier is
  actually wired in.

### `docs/features/README.md`

- [ ] Rephrase the security feature index entry to match the updated auth page.

### `AGENTS.md`

- [ ] Update the versioned OpenAPI entry-point path.

## Section D: Hygiene Appendix

Commands run:

- `git status --short --branch`
- `git log --oneline -n 10`
- `date +%F_%H:%M:%S`
- `sed -n '1,240p' README.md`
- `sed -n '1,260p' AGENTS.md`
- `sed -n '1,260p' docs/setup.md`
- `sed -n '1,260p' docs/overview.md`
- `sed -n '1,260p' docs/api.md`
- `sed -n '1,240p' docs/architecture.md`
- `sed -n '1,260p' api-spec/README.md`
- `sed -n '1,260p' api-spec/v0/openapi.yaml`
- `sed -n '1,260p' main.go`
- `sed -n '1,240p' api/app.go`
- `sed -n '1,220p' api/routes/router.go`
- `sed -n '1,220p' api/routes/status_router.go`
- `sed -n '1,220p' api/handlers/education_handler.go`
- `sed -n '1,220p' api/handlers/veteran_handler.go`
- `sed -n '1,220p' api/handlers/openapi_spec_handler.go`
- `sed -n '1,220p' api/handlers/status_handler.go`
- `sed -n '1,240p' api/middleware/middleware.go`
- `sed -n '1,220p' pkg/core/config.go`
- `sed -n '1,220p' mise.toml`
- `sed -n '1,260p' package.json`
- `ls -1 scripts`
- `rg -n '/api/edu|/status|api-spec/openapi.yaml|bundle-api-spec|validate-api-spec|lint-api-spec|lint-yaml-files|check-openapi-breaking|check-api-spec' README.md AGENTS.md api-spec/README.md docs/**/*.md`

Sensitive-file scan:

- `rg -n --hidden --glob '!docs/audit/**' --glob '!*node_modules*' 'BEGIN (RSA|OPENSSH|EC|DSA) PRIVATE KEY|AKIA[0-9A-Z]{16}|ghp_[A-Za-z0-9]{36}|xox[baprs]-[A-Za-z0-9-]+' .`
- No matches in tracked repository content outside archived audit reports.

Markdown validation:

- `pnpm exec markdownlint-cli2 README.md AGENTS.md api-spec/README.md docs/api.md docs/architecture.md docs/features/README.md docs/features/core/nsc-education.md docs/features/security/cognito-auth.md docs/overview.md docs/planning/public-api-data-model-design-first.md docs/setup.md docs/audit/2026-04-10_11:50:01.md`
- `python3` link-path check across the edited markdown set
- Result: markdownlint passed with `0 error(s)` and no broken local file links
  were found in the reviewed docs.

Hygiene verdict:

- `FAIL` before remediation because several reviewed docs contained
  evidence-backed accuracy drift.
- Post-fix validation of the edited markdown set passed, but the audit gate
  remains `FAIL` because this report records the pre-remediation state that
  triggered the update work.

## Section E: Deferred Watchlist (Non-blocking)

- If a real bearer-token or Cognito verifier is reintroduced in `api.New`,
  revisit `docs/api.md`, `docs/overview.md`, `docs/architecture.md`, and
  `docs/features/security/cognito-auth.md`.
- If shell wrapper scripts are later added back under `scripts/`, update
  `README.md`, `api-spec/README.md`, and
  `docs/planning/public-api-data-model-design-first.md`.
- If the artifact-serving route changes from `/api-spec/v1/verify`, update
  `docs/api.md` and `docs/overview.md`.
