# Documentation Accuracy Review (as of March 31, 2026)

## Section A: Executive Summary

**Report date:** `2026-03-31`
**Scope:** `root docs + docs/**/*.md`
**Overall accuracy score:** **88/100**
**Completion gate status:** **FAIL**

**Risk statement:**

- The main runtime documentation drift identified earlier is now resolved; the remaining blocker is contributor-facing setup guidance.
- Multiple docs still instruct contributors to run shell helper scripts such as `./scripts/bundle-api-spec` and `./scripts/lint-api-spec`, but those files are not present in the repository.
- Markdown hygiene and local doc-link validation are currently clean, so the remaining work is narrow and actionable.

## Section B: Severity-Ranked Findings

No `P1` findings were identified in this rerun.

| Severity | Doc location | Observed mismatch | Source-of-truth evidence on current branch | Recommended fix |
|---|---|---|---|---|
| `P2` | `README.md:28`; `README.md:33`; `README.md:36`; `README.md:39`; `README.md:42`; `README.md:50`; `api-spec/README.md:45`; `api-spec/README.md:48`; `api-spec/README.md:49`; `api-spec/README.md:50`; `docs/planning/public-api-data-model-design-first.md:160`; `docs/planning/public-api-data-model-design-first.md:164`; `docs/planning/public-api-data-model-design-first.md:167`; `docs/planning/public-api-data-model-design-first.md:170` | These docs tell contributors to run helper scripts such as `./scripts/bundle-api-spec`, `./scripts/validate-api-spec`, `./scripts/lint-api-spec`, `./scripts/lint-yaml-files`, and `./scripts/check-openapi-breaking`, but those script files are not present in `scripts/`. | `mise.toml:5`; `mise.toml:9`; `mise.toml:13`; `mise.toml:17`; `mise.toml:45`; `scripts/check-format-contract-files:1`; `scripts/check-json-schemas:1` | Update the docs to point readers to the supported `mise run ...` and `pnpm run ...` tasks, or add the missing shell wrappers so the documented commands actually exist. |
| `P3` | `docs/setup.md:6` | Setup docs still say the committed compose file provides “API and observability services only,” but the checked-in compose file also includes Redis as a first-class local dependency. | `docker-compose.yml:1`; `docker-compose.yml:12`; `docs/setup.md:80` | Reword the requirements note to describe the compose file as API, Redis, and observability services. |

## Section C: Update Backlog Checklist by Doc File

### `README.md`

- [ ] Replace the missing `./scripts/...` command examples with the supported `mise run ...` and `pnpm run ...` equivalents, or add the missing wrapper scripts.

### `api-spec/README.md`

- [ ] Replace the missing `./scripts/bundle-api-spec`, `./scripts/validate-api-spec`, and `./scripts/lint-api-spec` commands with currently supported commands.

### `docs/planning/public-api-data-model-design-first.md`

- [ ] Update the “Bundle, Validate, and Lint” examples so they reference commands that actually exist on this branch.

### `docs/setup.md`

- [ ] Reword the compose-file requirements sentence so it includes Redis instead of implying the stack is API plus observability only.

## Section D: Hygiene Appendix

### Commands run

- `date +%F_%H:%M:%S`
- `git status --short`
- `rg --files -g '*.md' . | sort`
- `rg -n -F '/status' README.md CONTRIBUTING.md COMMUNITY.md AGENTS.md SECURITY.md api-spec/README.md schema/README.md docs/**/*.md`
- `rg -n -F 'cookiecutter' README.md CONTRIBUTING.md COMMUNITY.md AGENTS.md SECURITY.md api-spec/README.md schema/README.md docs/**/*.md`
- `rg -n 'TODO|\{role\}|\{names\}|\{affiliations\}' README.md CONTRIBUTING.md COMMUNITY.md AGENTS.md SECURITY.md api-spec/README.md schema/README.md docs/**/*.md`
- `rg -n -F '8000' README.md CONTRIBUTING.md COMMUNITY.md AGENTS.md SECURITY.md api-spec/README.md schema/README.md docs/**/*.md`
- `rg -n -F '/api/edu' README.md CONTRIBUTING.md COMMUNITY.md AGENTS.md SECURITY.md api-spec/README.md schema/README.md docs/**/*.md`
- `rg -n -F '/health' README.md CONTRIBUTING.md COMMUNITY.md AGENTS.md SECURITY.md api-spec/README.md schema/README.md docs/**/*.md`
- `sed -n '1,220p' docs/features/core/edu-openapi-spec.md`
- `sed -n '1,260p' docs/features/core/nsc-education.md`
- `sed -n '1,260p' docs/planning/public-api-data-model-design-first.md`
- `ls scripts`
- `for p in scripts/bundle-api-spec scripts/validate-api-spec scripts/lint-api-spec scripts/lint-yaml-files scripts/check-openapi-breaking scripts/check-format-contract-files scripts/check-json-schemas; do test -e "$p" && echo "OK $p" || echo "MISSING $p"; done`
- `rg -n "./scripts/bundle-api-spec|./scripts/validate-api-spec|./scripts/lint-api-spec|./scripts/lint-yaml-files|./scripts/check-openapi-breaking|./scripts/check-format-contract-files|./scripts/check-json-schemas" README.md api-spec/README.md docs/**/*.md`
- `sed -n '1,220p' docker-compose.yml`
- `markdownlint-cli2 README.md CONTRIBUTING.md COMMUNITY.md AGENTS.md SECURITY.md api-spec/README.md schema/README.md "docs/**/*.md"`
- `rg -n --hidden --no-ignore "(BEGIN (RSA|EC|OPENSSH|DSA) PRIVATE KEY|AKIA[0-9A-Z]{16}|ASIA[0-9A-Z]{16}|ghp_[A-Za-z0-9]{36}|xox[baprs]-[A-Za-z0-9-]+|aws_secret_access_key|AIza[0-9A-Za-z_-]{35})" $(git ls-files '*.md')`
- `zsh -lc 'setopt null_glob; for f in README.md CONTRIBUTING.md COMMUNITY.md AGENTS.md SECURITY.md api-spec/README.md schema/README.md docs/**/*.md; do perl -ne '\''while(/\[[^\]]+\]\(([^)#]+)(?:#[^)]+)?\)/g){print qq{$ARGV\t$1\n} unless $1 =~ m{^(https?:|mailto:)} }'\'' "$f"; done | while IFS=$'\''\t'\'' read -r src link; do base=$(dirname "$src"); target="$base/$link"; if [ ! -e "$target" ]; then printf "%s -> %s\n" "$src" "$link"; fi; done'`

### Sensitive-file scan findings

- Scoped scan of tracked Markdown files found no live credential or private-key material.
- The only matches were prior audit reports that contain the scan regex as literal text.

### Markdown link/reference/path validation findings

- `markdownlint-cli2` passed for the audited Markdown scope with `0 error(s)`.
- Local relative-link/path validation found no missing file targets in the audited docs.

### Hygiene verdict

- **Non-blocker:** Markdown hygiene is clean, but the audit remains `FAIL` because unresolved contributor-facing `P2` command drift remains.

## Section E: Deferred Watchlist (Non-blocking)

- If the repository adds shell wrappers for contract tooling, update:
  - `README.md`
  - `api-spec/README.md`
  - `docs/planning/public-api-data-model-design-first.md`
- If the local compose stack changes again, update:
  - `docs/setup.md`
  - `README.md`
- If the public education endpoint becomes runtime-aligned with the v0 contract, update:
  - `docs/api.md`
  - `docs/overview.md`
  - `docs/features/core/nsc-education.md`
