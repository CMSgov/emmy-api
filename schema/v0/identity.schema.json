{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:emmy:schema:v0:identity",
  "title": "Identity",
  "description": "Basic identifying information for a person.",
  "type": "object",
  "additionalProperties": true,
  "required": ["firstName", "lastName", "dateOfBirth"],
  "properties": {
    "firstName": {
      "type": "string",
      "description": "Person's legal first name.",
      "examples": ["Lynette"]
    },
    "middleName": {
      "type": "string",
      "description": "Person's legal middle name when available.",
      "examples": ["Marie"]
    },
    "lastName": {
      "type": "string",
      "description": "Person's legal last name.",
      "examples": ["Oyola"]
    },
    "dateOfBirth": {
      "$ref": "urn:emmy:schema:v0:date",
      "description": "Person's date of birth."
    },
    "ssn": {
      "$ref": "urn:emmy:schema:v0:ssn",
      "description": "Social Security number when available."
    }
  }
}
